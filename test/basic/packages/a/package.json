{
  "name": "a",
  "scripts": {
    "build": "mkdir -p ./dist && cp ./index.js ./dist",
    "format": "echo \"format:a:start\"; sleep 1; echo \"format:a:end\"; exit 1"
  },
  "dependencies": {
    "b": "*"
  }
}
