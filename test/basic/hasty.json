{
  "pipeline": {
    "build": {
      "command": "build",
      "dependencies": ["format", "^build"]
    },
    "lint": {
      "command": "lint",
      "dependencies": ["format"]
    },
    "format": {
      "command": "format"
    },
    "test": {
      "command": "test",
      "dependencies": ["build", "lint"]
    }
  }
}
