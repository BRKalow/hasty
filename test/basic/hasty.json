{
  "pipeline": {
    "build": {
      "command": "build",
      "files": ["index.js"],
      "output": ["dist/**"],
      "dependencies": ["format", "^build"]
    },
    "lint": {
      "command": "lint",
      "dependencies": ["format"]
    },
    "format": {
      "command": "format"
    },
    "test": {
      "command": "test",
      "dependencies": ["build", "lint"]
    }
  }
}
